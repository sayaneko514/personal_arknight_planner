'use client';

import VideoBlock from './video-block';
import SideNav from './side-menu';

export const LandingPage = () => {
    return (
        <header>
            <VideoBlock />
            <SideNav />
        </header>
    );
}