"use client"

import SideNav from '../home-page/side-menu';
import AudioControl from '../home-page/audio-block';

export const AddOperatorsPage = () => {
    return (
        <header>
            <SideNav />
        </header>
    );
}